<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flash.framework.dynamic.datasource.dao.OmDao">

    <select id="selectId" parameterType="long" resultType="com.flash.framework.dynamic.datasource.model.Om">
      select id,`name` from o_m where id = #{id}
    </select>
</mapper>
